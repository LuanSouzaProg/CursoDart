void operadoresLogicos(){
  int n = 2;
  int n2 = 2;
  bool r = n <= n2 && n >= 10;

  // == igual
  // != diferente
  // >  maior
  // <  menor
  // >= maior ou igual
  // <= menor ou igual

  // && e(and)
  // || ou(or)
  // ! negação(not)

  print(r);

}